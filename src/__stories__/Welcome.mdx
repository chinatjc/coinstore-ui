import { Meta } from '@storybook/blocks';

<Meta title="Welcome" />

# React 组件库

## 安装

```bash
npm i @coinstore-ui/base
```

## 使用

```js
import { Button } from '@coinstore-ui/base';
```

## 备注

1. 组件库样式为`scss`，需要进行转译为`css`。

2. 组件库代码为`esnext`，需要按照`browserslist`的浏览器覆盖范围，进行转译。
